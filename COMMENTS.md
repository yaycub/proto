# Instructions to run Protocol Parser

1. Be sure to have the latest version of Node.js (14.8.0 or newer) installed
2. While in the project's directory (`/proto`), run `node proto.js` in your Terminal